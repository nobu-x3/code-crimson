{
  "configurations": {
    "launch": {
      "adapter": "CodeLLDB",
      "filetypes": [ "odin" ],
      "configuration": {
        "request": "launch",
        "program": "${workspaceRoot}/bin/code-crimson",
        "cwd": "${workspaceRoot}"
      }
    }
  }
}
